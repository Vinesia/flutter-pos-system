name: roastory_mobile
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  qr_flutter: ^4.1.0
  firebase_core: ^3.15.1
  cloud_firestore: ^5.6.11
  animated_check: ^1.0.4
  uuid: ^4.5.1
  intl: ^0.20.2
  fl_chart: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # Sesuaikan agar kompatibel dengan SDK 3.7.0

flutter:
  uses-material-design: true

  assets:
    - assets/latte.png
    - assets/americano.png
    - assets/mocha.png
    - assets/cappuccino.png
